/*

interface LoginDetails {
  username: string;
  password: string;
}

export default async function ({ username, password }: LoginDetails) {
  const res = await fetch("API LOGIN ENDPOINT HERE");
  const data = await res.json();
  // return logged in user data
}
*/
